import emails from './emails.json';
import onedrive from './onedrive.json';

export const MOCK_EMAILS = emails;
export const MOCK_ONEDRIVE = onedrive;

export default { emails: MOCK_EMAILS, onedrive: MOCK_ONEDRIVE };
